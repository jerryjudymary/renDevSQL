# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  pull_request: 
    branches: [ main ]

jobs:
  build:
  
    runs-on: ubuntu-18.04
    
    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - name: Setting .env
        run: |
          echo "AWS_SECRET_ACCESS_KEY="pb4O1ssCSb4RU9IsfeEmry9lqu+OKCXXxIGMARSv"
          echo "AWS_ACCESS_KEY_ID="AKIAXUCWUZI5MNBFFG72"
          echo "JWT_SECRET_KEY="Real_work"
          echo "JWT_SECRET_REFRESH="Real_work_refresh"
          echo "DB_HOST="database-1.cj2tg3os3jkh.ap-northeast-2.rds.amazonaws.com"
          echo "DB_PASSWORD="judymary123!"
          echo "TEST_HOST="database-1.cj2tg3os3jkh.ap-northeast-2.rds.amazonaws.com"
          echo "TEST_PASSWORD="judymary123!"
          echo "NODEMAILER_USER="rendev-notice@naver.com"
          echo "NODEMAILER_PASS="rendev99**"
          echo "REDIS_HOST="jerryAzureRedis.redis.cache.windows.net"
          echo "REDIS_PORT="6379"
          echo "REDIS_PASSWORD="Sz10L1Z22ljKInBAW10TGdhVuZb8LRKLzAzCaOdfEIU="
      - run: npm ci
      - run: npm run build --if-present
      - run: npm run test
